package com.example.project;

import android.app.AlertDialog;
import android.app.Dialog;
import android.content.Intent;
import android.os.Bundle;
import android.view.View;

import androidx.fragment.app.DialogFragment;


public class CustomDialogFragment extends DialogFragment {

 /*   public void dial(){
        Intent intent=new Intent(LeveloneParttwoActivity.this, NewgameActivity.class);
        startActivity(intent);
        finish();
    }

    public Dialog onCreateDialog(Bundle savedInstanceState, int x) {

        AlertDialog.Builder builder=new AlertDialog.Builder(getActivity());
        return builder.setTitle("Отлично!")
                .setMessage("Вы прошли 1 уровень!")
                .setPositiveButton("OK", dial)
                .create();
    }*/
}